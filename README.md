<h2>SeriesTracker</h2>

Install dependencies by running:

```npm install```

> In both root and frontend folders

<br />

**Run frontend:**

```(/frontend ) npm start```

**Run backend:**

```(root) npm run server```
